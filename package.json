{
  "name": "osnova-spec",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Спецификация формата проекта Osnova и контрактные схемы",
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "verify": "node scripts/verify-contracts.mjs",
    "generate": "node scripts/generate-contracts.mjs"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^5.7.3"
  }
}
